import React, { Component } from 'react'

class Saved extends Component {
    render () {
        return (
            <div>
                These are the saved recipes
            </div>
        )
    }
}

export default Saved